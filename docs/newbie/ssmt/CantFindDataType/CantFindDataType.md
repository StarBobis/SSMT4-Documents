# 找不到数据类型解决方案

1.将你此次提取使用的FrameAnalysis文件夹打包为.rar格式，然后发送给我

2.将你此次提取使用的DrawIB列表，以及提取的是哪个游戏的游戏名称发送给我

3.如果存在多个FrameAnalysis文件夹和对应的DrawIB列表，请说明对应关系，防止我找不到

4.在Dump前尽可能减少绿字IB的总数量，这样能让压缩文件尽可能少一些

5.发送文件时，尽可能通过QQ闪传发送

6.如果文件大小超出了QQ限制，可以分卷压缩后发送给我，千万不要给我网盘链接，否则可能下载好几天都下载不完

7.Dump的必须是游戏原本的模型，而不是打了Mod的模型

8.最好能开启Symlink特性再Dump，能进一步减少FrameAnalysis文件夹大小

9.如果对应游戏支持指定IB列表Dump，尽可能发我完整Dump，实在是太大时才发我指定IB列表Dump的结果

10.绝对不要擅自删除FrameAnalysis文件夹内的东西来减重，否则可能丢失信息导致我无法提取相关内容

11.当我添加完成数据类型后，我会推送新版本SSMT并和你说一声，到时候你只需要更新SSMT即可使用




