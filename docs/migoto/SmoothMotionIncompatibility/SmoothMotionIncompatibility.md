# ⚡ SmoothMotion 不兼容问题

在最新版的 **NVIDIA** 驱动中，新增了 **SmoothMotion** 这个选项。

## 🚫 问题描述

开启 **SmoothMotion** 会导致所有带有按键切换面板的 **Mod** 无法正常显示。

## 🛠️ 解决方案

如果你的 **3Dmigoto** 无法正常显示带面板的 **Mod** 的面板的话，可以试试在 **NVIDIA** 驱动中关闭这个选项。

> 💡 **提示**: 在 NVIDIA 控制面板中找到 SmoothMotion 设置并将其关闭即可恢复正常显示。